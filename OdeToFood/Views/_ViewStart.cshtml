@{
    // With this file being in the Views folder, this layout will affect all view.
    // You can override this by adding new _ViewStart.cshtml files in each folder. ie. /Home
    // Or, you can override it inside each file. Or set Layout to null to not use a layout.
    Layout = "~/Views/Shared/_Layout.cshtml";
}
